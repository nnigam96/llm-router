defaults:
  - router: hybrid
  - experts: default_experts
  - _self_

# System-wide settings
app:
  title: "LLM-Router"
  version: "1.0.0"
  host: "0.0.0.0"
  port: 8000
  debug: true

# Telemetry settings
forensics:
  log_path: "data/logs/routing_events.jsonl"
  enabled: true