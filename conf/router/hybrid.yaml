# Strategy: Hybrid (Cascading)
# Logic: Check keywords first. If no match, run embedding model.

name: "hybrid"
parameters:
  semantic_model: "all-MiniLM-L6-v2"
  semantic_threshold: 0.45  # Confidence needed to override default
  fallback_expert: "professor"